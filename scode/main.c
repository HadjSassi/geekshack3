#include<stdio.h>
#include<stdlib.h>
int main(){
    while (1<=abs(s)<=1000)
    {printf("give me your string so i can give you delicious substrings \n");}
    nb=0;
    for (i=0;i<=n;i++)
    {if(s[i]='c'&&s[i++]='h'&&s[i+=2]='a'&&s[i+=3]='k')
    {ch1="chak";
    i=K;
    }
   { for (j=0;j<=n;j++)
   if(s[j]='c'&&s[j++]='h'&&s[j+=2]='o'&&s[j+=3]='u'&&s[i+=4]='k'&&s[i+=5]='a')
    ch2="chouka";
        j=L;
    }}
    if (L>K)  nb++;
    printf("the number of delicious substrings in the given string is %d",nb);
    
    return 0;
} 